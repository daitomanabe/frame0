# Core ML Style Transfer

Native Apple ML example manifest that models camera preprocessing, Core ML
inference, temporal stabilization, and post-processed output.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/coreml_style_transfer/scene.yaml --json
cargo run -p frame0_cli -- graph examples/coreml_style_transfer/scene.yaml --json
```
