# timeline_sequencing

This scene defines a deterministic timeline contract with transport, clips, cues, keyframes, beat tracks, and replay metadata.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/timeline_sequencing/scene.yaml --json
cargo run -p frame0_cli -- schema export timeline --json
```
