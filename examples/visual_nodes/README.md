# visual_nodes

This scene demonstrates visual node contracts for feedback, instancing, particles, shader passes, text, and 2D/3D composition.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/visual_nodes/scene.yaml --json
cargo run -p frame0_cli -- schema export visual_node --json
```
