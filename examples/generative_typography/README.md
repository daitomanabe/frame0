# Generative Typography

Text and SDF typography pipeline with layout, glyph instances, feedback trails,
and audio/control-ready parameters.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/generative_typography/scene.yaml --json
cargo run -p frame0_cli -- graph examples/generative_typography/scene.yaml --json
```
