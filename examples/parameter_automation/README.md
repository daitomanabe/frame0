# parameter_automation

This scene defines parameter automation nodes for LFOs, ramps, presets, mapping, and smoothing.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/parameter_automation/scene.yaml --json
cargo run -p frame0_cli -- schema export automation --json
```

