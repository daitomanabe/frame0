# Multi-Camera Switching

Four synchronized camera inputs with color matching, motion scoring, automatic
switching, keying, and clean program/preview outputs.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/multi_camera_switching/scene.yaml --json
cargo run -p frame0_cli -- graph examples/multi_camera_switching/scene.yaml --json
```
