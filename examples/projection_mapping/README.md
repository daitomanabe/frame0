# Projection Mapping

Projection mapping scene manifest with calibration data, quad warp, edge blend,
masking, preview, and projector output.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/projection_mapping/scene.yaml --json
cargo run -p frame0_cli -- graph examples/projection_mapping/scene.yaml --json
```
