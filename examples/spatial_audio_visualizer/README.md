# Spatial Audio Visualizer

Multichannel audio analysis manifest for ambisonic/spatial audio, source
localization, instanced geometry, and audio-synchronized render output.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/spatial_audio_visualizer/scene.yaml --json
cargo run -p frame0_cli -- graph examples/spatial_audio_visualizer/scene.yaml --json
```
