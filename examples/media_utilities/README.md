# media_utilities

This scene models image sequences, movie playback, audio buffers, texture feedback, capture manifests, and playback manifests.

Run:

```bash
cargo run -p frame0_cli -- inspect examples/media_utilities/scene.yaml --json
cargo run -p frame0_cli -- schema export media_asset --json
```
