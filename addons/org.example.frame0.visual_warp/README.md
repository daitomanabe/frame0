# org.example.frame0.visual_warp

Example third-party schema-pack addon for a visual warp node.

Verify from the FRAME0 repository root:

```bash
cargo run -p frame0_cli -- inspect addons/org.example.frame0.visual_warp/examples/warp_scene.yaml --json
```
